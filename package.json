{
  "name": "mindbridge-rag",
  "private": true,
  "scripts": {
    "build": "node -e \"if(require('fs').existsSync('frontend'))process.chdir('frontend');require('child_process').execSync('npm run build',{stdio:'inherit'})\""
  }
}
